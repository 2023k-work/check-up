{
  "name": "@checkup/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vite.config.ts",
    "preview": "vite preview"
  }
}
